## FFmpeg binaries are not distributed here

Cortado builds its pinned FFmpeg SDK from source and bundles the runtime with
the application. The reproducible Docker recipe lives in the FCS repository at
`Cortado/packaging/ffmpeg`; this artifact repository no longer publishes
FFmpeg binary archives.
